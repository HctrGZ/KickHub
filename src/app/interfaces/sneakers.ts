export interface Sneakers {
    quantity: number;
    _id?: string,
    brandName: string;   
    name: string;         
    description: string;  
    image: string;        
    price: number;        
    color: string;        
    sizeRange: number[];  
}
